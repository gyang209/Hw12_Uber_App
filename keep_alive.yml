name: Keep App Alive
on:
  schedule:
    - cron: '0 0 */4 * *' # Runs every 4 days
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Streamlit App
        run: curl -I https://your-app-url.streamlit.app/
